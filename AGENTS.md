# Collaboration Notes

## Editing Preference

- Prefer rewriting the whole target file instead of applying small patch hunks.
- Avoid patch-style partial edits when a full-file rewrite is practical.
- For UI text changes, finish the related locale keys in the same pass.
